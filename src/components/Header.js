import React from "react";

const Header = () => {
  return (
    <header style={{ color: "white", backgroundColor: "gray" }}>
      <h1>Employee Directory</h1>
    </header>
  );
};

export default Header;
